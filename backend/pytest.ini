[pytest]
# Ensure the backend package root is importable regardless of how pytest is
# invoked (bare `pytest` does not add the rootdir to sys.path the way
# `python -m pytest` does).
pythonpath = .
testpaths = tests
