fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
pydantic==2.7.4
pydantic-settings==2.3.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
python-multipart==0.0.9
apscheduler==3.10.4
# Pin tzlocal (an APScheduler dependency): 5.3+ shipped broken/empty wheels that
# install but expose no module, breaking "import apscheduler". 5.2 is stable.
tzlocal==5.2
email-validator==2.1.1
openpyxl==3.1.5
# Web Push (VAPID) notifications to the staff PWA. Pure-Python wheels (no build
# step), so they install cleanly on the python:3.12-slim image. The feature
# stays dormant unless VAPID_PUBLIC_KEY / VAPID_PRIVATE_KEY are configured.
webpush==1.0.6
httpx==0.27.0
# Test-only: backend/tests/conftest.py spins up a real app instance against a
# throwaway SQLite DB per test (production uses asyncpg/PostgreSQL).
aiosqlite==0.22.1
