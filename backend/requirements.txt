fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
pydantic==2.7.4
pydantic-settings==2.3.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
python-multipart==0.0.9
apscheduler==3.10.4
# Pin tzlocal (an APScheduler dependency): 5.3+ shipped broken/empty wheels that
# install but expose no module, breaking "import apscheduler". 5.2 is stable.
tzlocal==5.2
email-validator==2.1.1
openpyxl==3.1.5
# httpx: required by Starlette's TestClient (see tests/conftest.py) and reused
# by the optional Web Push sender. Pure-Python wheel — installs on slim images.
httpx==0.27.0
# NOTE: the `webpush` library is intentionally NOT pinned here. Its current
# releases force major upgrades of pydantic/cryptography (e.g. 1.0.6 needs
# pydantic>=2.11.7), which we don't want to drag into production for an optional
# feature. The Web Push code (app/notifications.py) guards its import, so push
# simply stays disabled while the library is absent — everything else works.
# To enable push later, add a `webpush` pin whose deps match these versions
# (or bump pydantic/cryptography deliberately) and re-run the test suite.
# Test-only: backend/tests/conftest.py spins up a real app instance against a
# throwaway SQLite DB per test (production uses asyncpg/PostgreSQL).
aiosqlite==0.22.1
